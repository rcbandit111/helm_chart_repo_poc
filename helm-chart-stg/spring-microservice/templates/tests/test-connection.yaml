apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spring-microservice.fullname" . }}-test-connection"
  labels:
    {{- include "spring-microservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spring-microservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
